const quarterValue = (inputNumber) => {
    
    const quartered = inputNumber / 4
    return quartered
}

// Invoke the function and store the return value
const result = quarterValue(18)
// Log the return value
console.log(result)